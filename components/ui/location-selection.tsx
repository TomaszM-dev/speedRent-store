import React from "react";

const LocationSelection = () => {
  return (
    <div className="flex items-center justify-center w-full">
      This will be a form that contains rental search
    </div>
  );
};

export default LocationSelection;
